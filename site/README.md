# Deprecated — static Phase 1 prototype

The live website is now built with **Astro + Tailwind** from the repo root.

- **Dev:** `npm run dev` or `.\scripts\serve.ps1`
- **Build:** `npm run build` → output in `dist/`
- **Deploy:** Netlify publishes `dist/`

This folder is kept for reference only. Do not edit these files for website work.
